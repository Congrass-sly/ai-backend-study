# Python 字符串 面试级知识点归纳笔记
## 一、字符串核心特性（面试必背）
1. 字符串**有序、不可变、可迭代**；一旦创建不能原地修改，任何增删改操作都是**生成新字符串**。
2. 支持索引、切片；下标从 0 开始，负数下标从末尾倒着数。
3. 可嵌套、可拼接、可重复；能存字母、数字、符号、中文。
4. 属于**不可变类型**，不能做字典 value 以外无限制，**可以做字典 key**。

## 二、字符串切片（重中之重，万能核心）
语法：
```python
s[start:end:step]
```
规则：
- 左闭右开，取不到 end 下标
- start 省略默认从头，end 省略默认到末尾
- step 省略默认步长为1；步长为负**倒序截取**

高频面试/实战写法：
```python
s[:]         # 原样复制字符串
s[::-1]      # 字符串反转（必考）
s[2:5]       # 截取下标2到4
s[1::2]      # 从下标1开始，隔1个取1个
s[-3:]       # 取最后3个字符
```
面试一句话：**切片是字符串最通用操作，截取、反转、隔位取值全靠它，比多数内置函数更灵活**。

## 三、转义字符（面试+实战必考）
常用必记：
- `\n` 换行
- `\t` 制表符（空格缩进）
- `\r` 回车，光标回到**本行开头**，实现原地刷新（进度条、倒计时核心）
- `\\` 输出反斜杠
- `\' \"` 输出单引号、双引号

原始字符串 `r""`：
取消所有转义，路径、正则常用：
```python
path = r"C:\Users\Desktop"
```

## 四、字符串格式化（只精通 f-string 即可）
### 1、三种格式化了解即可
- `%` 旧式：语法繁琐、要记类型占位，**淘汰**
- `str.format()`：比%灵活，仍不如 f-string 简洁
- `f-string`：Python3.6+ 推荐，**面试&开发首选**

### 2、f-string 必会用法
```python
name = "张三"
age = 20

# 1. 嵌入变量
print(f"姓名：{name}")

# 2. 表达式直接运算
print(f"明年年龄：{age + 1}")

# 3. 控制小数位数
print(f"圆周率：{3.14159:.2f}")

# 4. 宽度对齐（进度条用过）
print(f"{5:3}%")
```

面试话术：
> 日常开发全部使用 f-string，语法简洁、性能更好、可读性强；老式%和format了解原理即可，不再业务使用。

## 五、必背高频内置方法（面试必考、必会手写）
### 1. 增减替换
- `replace(old, new)`：替换所有匹配子串
- `strip()`：去除首尾空格/指定字符；`lstrip()` 去左边、`rstrip()` 去右边

### 2. 分割与拼接
- `split(sep)`：按分隔符切割字符串，返回列表
- `join(可迭代对象)`：以当前字符串为分隔符，把列表拼接成字符串（高频考点）

### 3. 查找与统计
- `find(sub)`：找到返回下标，找不到返回 -1
- `index(sub)`：找不到直接报错
- `rfind() / rindex()`：从**右往左**查找
- `count(sub)`：统计子串出现次数

### 4. 判断开头结尾
- `startswith(sub)`：是否以指定子串开头
- `endswith(sub)`：是否以指定子串结尾

### 5. 大小写转换
- `upper()`：全部转大写
- `lower()`：全部转小写

### 6. 真伪判断（校验常用）
- `isdigit()`：是否纯数字
- `isalpha()`：是否纯字母/中文
- `isalnum()`：是否字母+数字组成

## 六、冷门方法（了解即可，不用死记、不用手写）
以下只需眼熟，面试不考、工作极少用：
1. 大小写样式：`capitalize()`、`title()`、`swapcase()`
2. 对齐填充：`center()`、`ljust()`、`rjust()`、`zfill()`
3. 拆分相关：`partition()`、`splitlines()`
4. 编码转换：`encode()`、`decode()`、`maketrans()`、`translate()`
5. 细碎判断：`islower()`、`isupper()`、`isspace()`、`isnumeric()`、`isdecimal()`

面试总结：**这类方法只需知道功能，不用背参数，用时查文档即可**。

## 七、partition 函数认知（面试简答版）
- 作用：从左找到**第一个**分隔符，固定返回三元组 `(前段, 分隔符, 后段)`
- 特点：保留分隔符，适合解析 `key=value` 键值对
- 定位：功能单一、灵活性不如切片和 split，**日常优先用切片/split，partition了解即可**

## 八、字符串不可变 面试常考题
1. 字符串不能通过下标原地修改 `s[0]='a'` 直接报错
2. 所有修改操作（拼接、替换、大小写）都是**生成新字符串**，原字符串不变
3. 不可变类型可作为字典 key、可哈希

## 九、面试一句话终极总结
Python 字符串核心考点集中在：**切片灵活操作、常用内置方法、转义字符应用、f-string 格式化**；其余冷门方法只需眼熟了解，无需死记硬背，掌握核心足以应付面试和实际开发。

